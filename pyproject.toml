[project]
name = "mfa-detection-platform"
version = "0.1.0"
description = "MFA detection platform monorepo"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = ["backend", "crawler", "ml", "common"]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "ruff>=0.8",
]
